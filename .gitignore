# Bảo vệ thông tin nhạy cảm — không đưa lên Git
# File .example là template an toàn → được phép commit
.env
.env.local
.env.staging
.env.production
.claude/

# Thư mục sinh ra khi chạy test — không cần lưu vào Git
node_modules/
playwright-report/
test-results/
reports/regression/

# Config xem test chạy chậm — dùng local, không commit
playwright.slowmo.js
